# Tools and actors placeholder

This directory is reserved for DAO-level tool and actor definitions. Tools expose useful operations; actors encapsulate repeatable multi-step workflows. n8n may implement some tools or actors through MCP, but it is not the DAO orchestrator. Do not create an n8n implementation folder until a defined tool or actor requires one.

Do not add a tool definition until its use case, interface, implementation location, required permissions, limitations, and safety considerations are understood. Do not store credentials or live service data here.
