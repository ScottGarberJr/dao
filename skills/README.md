# Skills placeholder

This directory is reserved for reusable DAO operating playbooks and automation skills. Add a dedicated skill only when its trigger, inputs, safety boundaries, and expected output are stable enough to reuse.

No skills are defined or enabled yet. Add one only after the relevant team has jointly defined the repeatable process it should support.
