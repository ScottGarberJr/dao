# n8n workflow placeholder

Store exported, sanitized workflow definitions here only after review. Exclude credentials, webhook URLs containing secrets, execution exports, and real email content. Every workflow must implement the V1 Outlook/n8n contract and the guardrails in `contracts/outlook-n8n.contract.yaml`.
